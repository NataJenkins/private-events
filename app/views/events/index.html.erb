<p id="notice"><%= notice %></p>

<h1>Events</h1>

<table>
  <thead>
    <tr>
      <th>Date</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% past = Array.new %>
    <% future = Array.new %>
    <% @events.each do |event| %>
        <% past.push(timer_e(event.date, event)) %>
        <% future.push(timer2_e(event.date, event)) %>
    <% end %>

    <td><strong> Past <strong></td>
    <% past = if_nil(past) %>
    <% past.each do |shark| %>
      <tr>
          <td><%= shark.date %></td>
          <td><%= link_to 'Show', shark %></td>


      </tr>
    <% end %>

        <td><strong> Future <strong></td>
        <% future = if_nil(future) %>
        <% future.each do |shark| %>
        <tr>
          <td><%= shark.date %></td>
          <td><%= link_to 'Show', shark %></td>

        </tr>
        <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Event', new_event_path %>
<%= link_to 'Back', root_path %>
